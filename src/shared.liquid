<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
  .screen {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  }

  .view {
    border: none !important;
    box-shadow: none !important;
  }

  .view::after {
    display: none !important;
  }

  .row {
    display: flex;
    align-items: stretch;
    padding: 7px 0;
    border-bottom: 1px solid #ddd;
  }

  /* Shared left-side slot — holds either the accent bar or the calendar icon */
  .row__lead {
    width: 26px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    margin-right: 8px;
  }

  /* Vertical accent bar for timed events */
  .row__bar {
    width: 3px;
    background: #000;
    border-radius: 2px;
    align-self: stretch;
  }

  .row__content {
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .row__title {
    font-size: 0.95rem;
    font-weight: 600;
    line-height: 1.3;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .row__desc {
    font-size: 0.72rem;
    color: #666;
    margin-top: 2px;
    line-height: 1.2;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Right-side time column */
  .row__time {
    flex-shrink: 0;
    text-align: right;
    margin-left: 12px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .row__time-start {
    font-size: 0.78rem;
    font-weight: 500;
    line-height: 1.3;
    white-space: nowrap;
  }

  .row__time-end {
    font-size: 0.72rem;
    color: #666;
    line-height: 1.2;
    white-space: nowrap;
  }

  .row__allday-label {
    font-size: 0.72rem;
    color: #666;
    white-space: nowrap;
  }

  .empty {
    font-size: 0.88rem;
    color: #aaa;
    font-style: italic;
    text-align: center;
    padding-top: 24px;
  }
</style>
